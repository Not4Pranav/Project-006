# ============================================================
# Multi-Sniper configuration
# 1) Copy this file:   cp .env.example .env
# 2) Add only your own values to .env or your host's secret manager
# 3) This template deliberately contains NO bot tokens, proxy credentials, or API keys
# 4) NEVER commit or share your real .env file
# ============================================================

# --- REQUIRED ------------------------------------------------
# Bot token: Discord Developer Portal -> your app -> Bot -> Reset Token
# Leave blank in this template. Set it only in your private .env / host secrets.
DISCORD_TOKEN=

# --- Channel targeting ---------------------------------------
# The single text channel the bot watches. Blank = watch every channel.
# Get an ID: Discord Settings -> Advanced -> Developer Mode ON,
# then right-click the channel -> Copy Channel ID
TARGET_CHANNEL_ID=

# Optional: separate channel where "available" hits get logged
LOG_CHANNEL_ID=

# --- Checking behaviour --------------------------------------
# Per-outbound-request timeout in seconds. It is clamped below the response
# budget so one slow platform cannot delay the reaction indefinitely.
CHECK_TIMEOUT=3

# Hard valid-message budget from Discord event handling through reactions.
# The bot reserves time for Discord's reaction REST call; values are clamped
# below 5 seconds in code.
RESPONSE_BUDGET_SECONDS=4.5
# Cap for each reaction API call. Available-platform reactions run in parallel.
REACTION_TIMEOUT=0.75

# Anti-abuse cooldown: max checks per user per window
USER_MAX_CHECKS=3
USER_WINDOW_SECONDS=60

# Cache repeat lookups for this many seconds (protects you from rate limits)
RESULT_CACHE_TTL=300

# --- Discord username check ----------------------------------
# Discord has NO public username-availability API.
#   off    = skip the Discord check (recommended, honest default)
#   probe  = query an explicit, authorized checker URL you control. It must
#            include {username}; blank DISCORD_PROBE_URL is skipped.
#            discord.com/<username> is NOT a valid availability endpoint.
DISCORD_CHECK_MODE=off
DISCORD_PROBE_URL=
# Optional auth sent ONLY to DISCORD_PROBE_URL. Keep the actual token blank here.
# Change the header/scheme only if your authorized checker requires it.
DISCORD_PROBE_TOKEN=
DISCORD_PROBE_TOKEN_HEADER=Authorization
DISCORD_PROBE_TOKEN_SCHEME=Bearer

# --- Networking (optional) -----------------------------------
# Route outbound platform checks through a user-supplied HTTP(S) proxy.
# Leave blank for a direct connection. Credentials belong only in your private
# .env / host secrets, never in this tracked template. SOCKS needs aiohttp-socks.
PROXY_URL=
