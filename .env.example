# ============================================================================
# Multi-Sniper v3.0 configuration
# ============================================================================
# HOW TO USE THIS FILE
#   1) Copy it to a PRIVATE file named `.env` (never commit that file):
#
#          cp .env.example .env
#
#   2) Fill in your own values. Everything except DISCORD_TOKEN has a safe
#      default, so you can leave most of this file untouched.
#   3) `.env` is git-ignored and is loaded automatically by bot.py at startup
#      (python-dotenv). Hosting providers: set these as environment variables
#      in your host's secret manager instead of a file.
#
# SAFETY
#   - Keep real tokens, passwords, API keys, and proxy credentials out of git.
#   - This template deliberately ships with every credential field blank.
#   - bot.py redacts credentials from log output and validates configuration
#     before connecting to Discord.
# ============================================================================

# --- REQUIRED ---------------------------------------------------------------
# The bot cannot start without this.
# Where to get it: Discord Developer Portal -> your app -> Bot -> Reset Token.
# Keep the token on a single line; no quotes, no spaces, no line breaks.
DISCORD_TOKEN=

# --- Channel targeting (optional) ------------------------------------------
# The single text channel the bot watches. Blank = watch every channel the bot
# can see. If both this and LOG_CHANNEL_ID are blank, the bot still works, it
# just answers in every visible channel.
# To find an ID: Discord -> Settings -> Advanced -> Developer Mode ON,
# right-click the channel -> Copy Channel ID.
TARGET_CHANNEL_ID=

# Optional: when a name is found free, post a quick mention + name here.
# Blank = logging off. Use a private channel only the bot can see.
LOG_CHANNEL_ID=

# --- How the bot answers ----------------------------------------------------
#   reply  = post a readable list as a reply (default)
#              Minecraft: Available
#              guns.lol: Unavailable
#              Instagram: Unknown
#   react  = add one emoji per free platform to the original message
#   both   = do both
RESPONSE_MODE=reply

# Minimum gap (seconds) between live edits of that reply while results stream
# in. The first paint is always immediate; this only throttles follow-ups so a
# lookup cannot burn Discord's edit rate limit. Range: 0.1-5.
REPLY_EDIT_INTERVAL=0.7

# Show platforms that are switched off (e.g. Discord when the mode is "off").
REPLY_INCLUDE_SKIPPED=false

# Ping the requester in the reply. Off by default: the reply is already
# attached to their message, and pings get noisy in a busy channel.
REPLY_MENTION_AUTHOR=false

# --- Free 24/7 hosting ------------------------------------------------------
# Most free hosts only keep a service alive if it binds an HTTP port. Setting
# PORT (or KEEPALIVE_PORT) starts a tiny health endpoint at / and /health
# alongside the bot. It exposes status only: no secrets, no controls.
# Leave blank on a VPS or your own machine. See CLOUD_SETUP.md.
# PORT=8080
# KEEPALIVE_PORT=

# --- Checking behaviour -----------------------------------------------------
# Per-outbound-request timeout (seconds). Clamped below RESPONSE_BUDGET_SECONDS
# so one slow platform cannot delay the reaction. Keep small; 3 is a good default.
CHECK_TIMEOUT=3

# Per-socket CONNECT cap inside CHECK_TIMEOUT (seconds). A proxy or host that
# cannot even open a connection inside this window is abandoned so the retry
# lands on the next proxy, instead of burning the entire check budget. Healthy
# TLS connects land in tens of milliseconds, so this never trims a good link.
CONNECT_DEADLINE=2

# Hard budget (seconds) allowed from a valid message through checks + reactions.
# Clamped in code to [0.5, 4.8] so a typo cannot push reactions past 5 seconds.
RESPONSE_BUDGET_SECONDS=4.5

# Cap (seconds) for each Discord reaction REST call. Free-platform reactions
# run concurrently so this is a per-call cap, not a sum.
REACTION_TIMEOUT=0.75

# Anti-abuse cooldown: how many checks one user may fire in USER_WINDOW_SECONDS.
# The check after that gets the hourglass reaction (⏳). Defaults are sub-second
# so checking feels instant; raise USER_WINDOW_SECONDS only if you need a
# stricter anti-spam guard.
USER_MAX_CHECKS=5
USER_WINDOW_SECONDS=0.5

# --- Speed ------------------------------------------------------------------
# Add each platform's emoji the MOMENT that platform answers, instead of
# waiting for the slowest one. This is the single biggest win in perceived
# speed: time-to-first-reaction drops from "slowest check" to "fastest free
# check" (measured 1600ms -> 120ms with one slow straggler).
# Trade-off: emojis appear in completion order rather than platform order.
# Set to false for the old batched behaviour (all emojis at once, in order).
STREAM_REACTIONS=true

# Pre-open a pooled TLS connection to every platform host at startup, so the
# first real lookup does not pay DNS + TCP + TLS setup (~100-300ms per host).
# Runs in the background and never delays login; failures are ignored.
PREWARM_CONNECTIONS=true

# --- Busy channels ----------------------------------------------------------
# Every message is answered independently, as a reply to its own message, so
# many members can check many different usernames at the same moment.
#
# Outbound connection pool. One lookup opens up to 8 connections, so a channel
# where a dozen people paste names at once needs a much larger pool than a
# single lookup does. Too small a pool does not fail loudly: requests queue
# until they blow their deadline and report "Unknown". Range 8-5000 / 2-1000.
HTTP_POOL_LIMIT=200
HTTP_POOL_LIMIT_PER_HOST=40

# When several members paste the SAME username while a check for it is already
# running, they all wait on that one check instead of starting their own.
# Everyone still gets their own reply; the sites are just hit once.
COALESCE_DUPLICATES=true

# --- Fallback source --------------------------------------------------------
# When a platform's own endpoint stops answering usefully (Cloudflare wall,
# rate limit, network error), ask instantusername.com instead of reporting
# "Unknown". If an endpoint simply stalls past 1s, the second opinion is
# hedged in parallel instead of waiting out the timeout - with a 250ms grace
# window in which the platform's own verdict still overrules the aggregator.
# It only overrides the result when it is itself definitive, and always
# inside the same response budget. Covers GitHub, Steam, Reddit, Instagram
# and Twitter/X (plus anything else instantusername lists at startup). Set to
# false to keep usernames from leaving your host except to the platforms
# themselves.
INSTANTUSERNAME_FALLBACK=true

# --- Smart caching ----------------------------------------------------------
# Reuse the answer for a given name. A cache hit answers in microseconds with
# no network calls at all, which is what makes repeat checks feel instant.
# Different TTLs for taken vs available:
#   - TAKEN names are cached longer (they rarely free up quickly)
#   - AVAILABLE names are cached shorter (they might get sniped fast)
# RESULT_CACHE_TTL is the base value: leave the other two blank and they
# default to 2x it (taken) and 0.4x it (available). Set them to 0 to disable.
RESULT_CACHE_TTL=300
CACHE_TTL_TAKEN=600
CACHE_TTL_AVAILABLE=120

# Hard ceiling on cached usernames. The cache is pruned on write (stale first,
# then oldest) so a busy server cannot grow it without bound. Range: 64-1000000.
CACHE_MAX_ENTRIES=5000

# --- Extra platforms --------------------------------------------------------
# Enable or disable the extra platform checks (GitHub, Steam, Reddit, Instagram,
# Twitter/X). Set to "false" to only check Minecraft + guns.lol + Discord.
ENABLE_EXTRA_PLATFORMS=true

# Per-platform opt-out, comma-separated display names. A disabled platform is
# never even scheduled: it costs no sockets, no proxy-pool slots, and no reply
# rows. Checks all run in parallel under one deadline, so disabling does not
# make the remaining checks faster — it makes results cleaner when a platform
# hard-walls automation. On free datacenter hosting (Render free tier etc.)
# Reddit and Twitter/X almost always come back Unknown behind bot walls, so
# most cloud deploys should use:
#   DISABLED_PLATFORMS=Reddit,Twitter/X
# Casual spellings ("twitter", "X") are accepted. Valid names: Minecraft,
# guns.lol, Discord, GitHub, Steam, Reddit, Instagram, Twitter/X.
# Leave empty to check everything:
DISABLED_PLATFORMS=

# --- Discord username check (optional, safe default: off) -------------------
# No mode ever logs in to Discord and none sends your bot token anywhere
# other than Discord's own gateway. The website modes (instantusername,
# combined, dnsrobot) never contact Discord's servers directly: they read
# instantusername.com and/or load dnsrobot.net's public checker page, which
# does its own checking from the browser.
#
#   off       = skip the Discord check (recommended default)
#   dnsrobot  = load DNS Robot's username-checker page in Chromium and read
#               its rendered Discord result
#   instantusername = ask instantusername.com's Discord service over plain
#               HTTP (no browser needed, works on small free hosts)
#   combined  = race instantusername.com and DNS Robot together; the DNS
#               Robot page verdict wins on disagreement, the web answer is
#               used when no browser is installed
#   account   = POST {"username": "..."} to the account API below (Discord's
#               unauthenticated eligibility route; opt-in only, no login)
#   account_api = compatibility alias for account
#   probe     = GET an explicit authorized checker URL (200=taken, 404=free)
DISCORD_CHECK_MODE=off

# dnsrobot loads the fixed public page at
# https://dnsrobot.net/username-checker?u=<username> in an isolated Chromium
# context. The page makes its own credential-free browser request; no DNS Robot
# URL/token or account/probe credential is needed. Playwright is an optional
# dependency (not in requirements.txt); install it for this mode:
#   python -m pip install 'playwright>=1.48,<2'
#   python -m playwright install chromium
# A page block, rate limit, timeout, or ambiguous result remains UNKNOWN.

# Used by DISCORD_CHECK_MODE=account. The default is Discord's first-party
# username eligibility route. Set this only when an authorized account API
# gateway exposes the same JSON contract. An account-scoped endpoint can be
# supplied explicitly, e.g. https://discord.com/api/v10/users/@me/pomelo-attempt.
DISCORD_ACCOUNT_API_URL=

# Optional credential sent ONLY to DISCORD_ACCOUNT_API_URL. Leave blank for
# Discord's unauthenticated registration-flow route. Do not put a personal
# Discord client token here; use only an authorized API/OAuth credential.
DISCORD_ACCOUNT_API_TOKEN=
DISCORD_ACCOUNT_API_TOKEN_HEADER=Authorization
DISCORD_ACCOUNT_API_TOKEN_SCHEME=Bearer

# Used when DISCORD_CHECK_MODE=probe. Leave blank to skip the external probe.
# Example:
#   https://my-checker.example/name/{username}
# The bot substitutes the username into {username} and treats 200 = taken,
# 404 = free, 401/403/429 = unknown.
DISCORD_PROBE_URL=

# Optional credential sent ONLY to DISCORD_PROBE_URL, never logged or committed.
# Keep blank unless your checker requires auth.
DISCORD_PROBE_TOKEN=

# Header that carries the probe token above (e.g. Authorization or X-API-Key).
DISCORD_PROBE_TOKEN_HEADER=Authorization

# Prefix before the probe token (e.g. Bearer). Set blank to send the raw token.
DISCORD_PROBE_TOKEN_SCHEME=Bearer

# --- guns.lol check mode (optional) -----------------------------------------
#   page    = check guns.lol via plain HTTP (default, ~300 MB less RAM)
#   browser = load guns.lol in Playwright Chromium to defeat Cloudflare
#             challenges. Reuses the same browser instance and semaphore as
#             the Discord dnsrobot/combined path.
#
# WARNING: browser mode adds ~300 MB of RAM and ~100 MB of disk (Chromium).
#          On 512 MB hosts (Render, small free tiers) it may trigger exit
#          code 137 (OOM). Flip back to page mode if the host runs out of
#          memory.
#
# Install Playwright for browser mode:
#   pip install 'playwright>=1.48,<2'
#   python -m playwright install chromium
GUNSLOL_CHECK_MODE=page

# --- Proxy pool (optional) --------------------------------------------------
# Route outbound platform checks through proxies. THREE sources, merged in
# this order with duplicates dropped: PROXY_URL, PROXY_URLS, then PROXY_FILE.
#
# EASIEST: put your vendor's list in proxies.txt next to bot.py, one per line.
# It is loaded automatically - nothing to set here - and it is gitignored so
# credentials never reach a commit. See proxies.txt.example.
#   PROXY_FILE=proxies.txt      (default; set another path, or blank to disable)
#
# SINGLE PROXY (backward compatible):
#   PROXY_URL=http://user:pass@proxy.example:8080
#
# PROXY POOL (rotation + failover):
#   PROXY_URLS=http://proxy1:8080,http://proxy2:8080,http://user:pass@proxy3:8080
#
# Every common vendor format is accepted anywhere a proxy is listed and is
# normalised for you - all five of these mean the same thing:
#   1.2.3.4:8080 | 1.2.3.4:8080:user:pass | user:pass@1.2.3.4:8080
#   user:pass:1.2.3.4:8080 | http://user:pass@1.2.3.4:8080
# SOCKS proxies are not supported and are rejected at startup rather than
# silently ignored (which would leak this host's real IP).
#
# The proxy pool provides:
#   - Round-robin rotation across healthy proxies, resolved PER REQUEST, so the
#     eight platform checks in one lookup spread across eight different IPs
#   - Live health reporting: a proxy that fails a real check is benched
#     immediately, and the retry lands on the next proxy in the rotation
#   - Concurrent health checks every 30 seconds (all proxies probed at once)
#   - Dead-proxy cooldown with automatic recovery after 60 seconds
#
# You can use BOTH: PROXY_URL is added to the pool automatically.
# Leave blank for a direct connection.
# Credentials belong only in your private .env / host secrets, never in git.
PROXY_URL=
PROXY_URLS=
PROXY_FILE=proxies.txt

# --- Remote proxy list ------------------------------------------------------
# Big public lists (tens of thousands of entries) are fetched rather than
# stored: they are too large for a repo and go stale within hours. The
# download, sampling and verification all run in the BACKGROUND after the bot
# is already online - a slow list host can never delay the gateway login,
# and the pool is upgraded in place while lookups are being answered.
# Google Drive "view" links and GitHub "blob" links are rewritten to their
# direct-download form automatically. Set blank to disable remote loading.
PROXY_LIST_URL=https://drive.google.com/file/d/1-Go3mD7uZ-2j5-YoytjsHJAKbPA7RDsN/view?usp=drivesdk

# Downloaded list is cached here so restarts are instant and work offline.
PROXY_CACHE_FILE=.proxy-cache.txt
# Seconds before the list is downloaded again (default 6 hours).
PROXY_LIST_TTL=21600
PROXY_LIST_TIMEOUT=20

# Most proxies allowed in the rotation. A ceiling, not a target: every entry
# costs a little memory and a slice of each health sweep. A big pool is still
# genuinely useful against per-IP rate limits, so the ceiling sits well above
# the floor below. The sample is spread across the whole list, not the first
# N. Locally configured proxies are never sampled away.
PROXY_MAX_POOL=2000

# How many WORKING proxies to end up with. Because a public list is mostly
# dead, verification keeps pulling fresh candidates from it until this many
# have answered - at the ~1% hit rate a free list gives, reaching 1,000 means
# probing about 100,000 entries, which took 3m45s in rehearsal.
# The search runs in the background, so the bot answers messages meanwhile.
# On a small free host (512 MB, 0.1 CPU) try 200 with a matching
# PROXY_VERIFY_CONCURRENCY: the search costs CPU, the pool itself barely does.
PROXY_MIN_POOL=1000
# Wall-clock ceiling for that search. It stops early once the target is met.
PROXY_VERIFY_MAX_SECONDS=900

# Probe every proxy once at startup and keep only the ones that answer.
# Public lists are mostly dead on arrival; without this the rotation would
# hand out corpses and every lookup would pay a timeout before failing over.
# If NOTHING answers, the pool is kept and you get a warning instead - an
# empty pool would silently mean direct, unproxied traffic.
PROXY_VERIFY_ON_START=true
# Verification uses its own connector, so these never compete with live
# lookups. Working free proxies answer within a couple of seconds; the rest
# are timeouts, so wide-and-short finds them much faster than narrow-and-slow.
# Each in-flight probe holds one file descriptor, and the usual Linux default
# is 1024: the bot raises its own soft limit, and if the host refuses it
# narrows the probe width to fit and logs a warning instead of crashing.
PROXY_VERIFY_CONCURRENCY=1000
PROXY_VERIFY_TIMEOUT=5

# What a proxy must be able to fetch to count as working. Deliberately HTTPS:
# every real check is HTTPS, so a proxy that cannot CONNECT is useless here
# even if it happily serves plain HTTP.
PROXY_PROBE_URL=https://api.mojang.com

# Parallelism of the periodic 30-second sweep of the already-verified live
# pool. Wide enough to get through a pool of thousands, still far gentler
# than the startup search - these proxies already answered once.
PROXY_HEALTH_CONCURRENCY=200

# Skip entries on well-known SOCKS ports (1080, 4145, 9050, ...). aiohttp
# cannot speak SOCKS, so on a scraped list these are just wasted probes.
PROXY_SKIP_SOCKS_PORTS=true

# What to do when EVERY proxy is benched.
#   false (default) = keep using proxies (reset the failure counters and retry)
#   true            = fall back to a direct, unproxied connection
# The default is false on purpose: a direct fallback would expose this host's
# real IP to the platforms, which is usually the exact reason proxies were
# configured. Only set this to true if you treat proxies as a speed
# optimisation rather than a privacy boundary.
PROXY_ALLOW_DIRECT_FALLBACK=false
