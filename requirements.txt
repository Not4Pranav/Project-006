# Multi-Sniper runtime dependencies.
# Python 3.9+ required (the code uses modern type-hint syntax).
# Install with:  pip install -r requirements.txt

discord.py>=2.3,<3
aiohttp>=3.9,<4
python-dotenv>=1.0,<2
