# Multi-Sniper runtime dependencies.
# Python 3.10+ required (the code uses modern type-hint syntax).
# Install with:  python -m pip install -r requirements.txt

discord.py>=2.3,<3
aiohttp>=3.9,<4
python-dotenv>=1.0,<2

# ---------------------------------------------------------------------------
# OPTIONAL - only needed for DISCORD_CHECK_MODE=dnsrobot.
# Keeping it out of the default install makes setup much faster (no browser
# toolchain to download) on every host. Install it when you enable that mode:
#
#   python -m pip install 'playwright>=1.48,<2'
#   python -m playwright install chromium
# ---------------------------------------------------------------------------
# playwright>=1.48,<2
